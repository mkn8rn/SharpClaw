# SharpClaw environment overrides (systemd EnvironmentFile=)
# Copy to /etc/sharpclaw/.env and set values appropriate for the host.
#
# Documentation for each key lives in the repo's .env.template.

ASPNETCORE_ENVIRONMENT=Production
# DOTNET_ROOT=/usr/lib/sharpclaw/api
# Database__ConnectionString=
# Auth__JwtSigningKey=
