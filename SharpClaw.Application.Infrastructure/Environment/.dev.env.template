{
  // SharpClaw Development Environment Overrides
  // Values here override .env and are only loaded in Development mode.

  // ── Encryption ─────────────────────────────────────────────────
  // Override encryption settings for development.
  // Key: Base64-encoded 256-bit key. If unset, auto-generated.
  // ⚠️  Invalid keys crash the backend at startup.
  // EncryptDatabase: encrypt JSON data files on disk (default: true).
  // EncryptProviderKeys: encrypt provider API keys in DB (default: true).
  //   ⚠️  Setting EncryptProviderKeys to false stores secrets in plaintext.
  //"Encryption": {
  //  "Key": "",
  //  "EncryptDatabase": "true",
  //  "EncryptProviderKeys": "true"
  //},

  // ── JWT ────────────────────────────────────────────────────────
  //"Jwt": { "Secret": "" },

  // ── Database ───────────────────────────────────────────────────
  //"ConnectionStrings": { "Postgres": "Host=localhost;Database=sharpclaw_dev;Username=sharpclaw;Password=devpassword" },
  //"Database": {
  //  "EnableDetailedErrors": "true",
  //  "EnableSensitiveDataLogging": "false"
  //},

  // ── Serilog ────────────────────────────────────────────────────
  // Development overrides for the core API.
  // Example: lower the default level while keeping noisy frameworks filtered.
  //"Logging": {
  //  "Serilog": {
  //    "Enabled": "true",
  //    "ConsoleEnabled": "true",
  //    "FileEnabled": "true",
  //    "RequestLoggingEnabled": "true",
  //    "MinimumLevel": "Debug",
  //    "MicrosoftMinimumLevel": "Information",
  //    "AspNetCoreMinimumLevel": "Information",
  //    "EntityFrameworkCoreMinimumLevel": "Information"
  //  }
  //},

  // ── Admin Seed ──────────────────────────────────────────────────
  // ReconcilePermissions (default true) keeps Admin permissions current
  // when modules add resource types or global flags.
  "Admin": { "Username": "admin", "Password": "123456", "ReconcilePermissions": "true" },

  // ── Local Inference (LLamaSharp) ─────────────────────────────
  // Override for development — e.g. GPU layers or disable KeepLoaded
  // to free memory between test runs.
  //"Local": { "GpuLayerCount": "-1", "ContextSize": "8192", "KeepLoaded": "true", "IdleCooldownMinutes": "5" },

  // ── External Modules ──────────────────────────────────────────────
  // Additional modules loaded from absolute paths on disk.
  // Modules loaded at runtime are automatically appended here as disabled;
  // enable them after confirming they load cleanly.
  //"ExternalModules": [
  //  { "Path": "C:\\modules\\Custom.Module\\bin\\Debug\\net10.0", "Enabled": true }
  //],

  // ── Unique Names ────────────────────────────────────────────────
  //"UniqueNames": { "Agents": "true", "Channels": "true", "Providers": "true", "Models": "true", "Roles": "true", "Contexts": "true" },

  // ── Modules ────────────────────────────────────────────────────
  // All bundled modules enabled by default in development.
  "Modules": {
    "CrashOnExternalModuleLoadFailure": "true",
    "sharpclaw_agent_orchestration": "true",
    "sharpclaw_context_tools": "true",
    "sharpclaw_editor_common": "true",
    "sharpclaw_metrics": "true",
    "sharpclaw_module_dev": "true",
    "sharpclaw_providers_anthropic": "true",
    "sharpclaw_providers_google": "true",
    "sharpclaw_providers_llamasharp": "true",
    "sharpclaw_providers_ollama": "true",
    "sharpclaw_providers_openai_compat": "true",
    "sharpclaw_vs2026_editor": "true",
    "sharpclaw_vscode_editor": "true"
  }
}
